# RetentIQ — segredos para infra/docker/docker-compose.yml
# Copie para infra/docker/.env antes de rodar `docker compose up`.
# NUNCA commite o .env real — apenas este .env.example.

POSTGRES_PASSWORD=troque-esta-senha
JWT_SECRET=troque-este-valor-antes-de-usar-em-producao
DEMO_ADMIN_EMAIL=admin@retentiq.dev
DEMO_ADMIN_PASSWORD=troque-esta-senha
GRAFANA_ADMIN_PASSWORD=troque-esta-senha
